#Modules  are ways of organizing our python codes into different files while they are being linked together,instead of writing the codes in a single file
#We recall each file to be linked by using the import function e.g import price

#A package is a folder created with some python files or modules embedded in it
#We call a module from a package with the use of import.shopping(package name).price_list(module name)
#Another method of importimg modules from packages is the use of (from name of package.sub_package.module import function.E.g from shopping.more_shopping.shopping_cart import sell